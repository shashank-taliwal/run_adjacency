pipelines:

  - name: pipelineConditionVariable
    steps:
      - name: cv1step
        type: Bash
        execution:
          environmentVariable
          onExecute:
            - echo "executing step..."
            - add_run_variables "key=value"

      - name: cv2step
        type: Bash
        configuration:
          environmentVariables:
              new_env:
                default: 2
          condition: 'new_env == 2'
          inputSteps:
            - name: cv1step
        execution:
          onExecute:
            - echo "executing step..."
            - echo $key
            - sleep 10
            
      - name: cv1step
        type: Bash
        configuration:
          condition: 'key == "value"'
          inputSteps:
            - name: cv2step
        execution:
          onExecute:
            - echo "executing step..."
            - echo $key
            - sleep 10
